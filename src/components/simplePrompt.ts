export async function simplePrompt(message: string, _default: string) {
  // TODO
  console.warn("message", message);



  return new Promise(function (resolve) {
    const answer = "todo";
    resolve(answer);
  });
}
